# 需求文档：A 股申万行业成交与买卖分析

> 版本：**v3.6**（页面 2 **概念 Tab** + 原子资金流 + 补数 **≤400 交易日**）  
> 状态：Phase 1 ✅ · Phase 2/3/3b ✅（持续迭代）  
> 必盈接入：[BIYING_API.md](./BIYING_API.md)  
> 实现方案：[IMPLEMENTATION_PLAN.md](./IMPLEMENTATION_PLAN.md)

---

## 1. 项目目标

基于 **必盈 API**，构建 A 股 **成交额 + 主买/主卖** 与 **ETF 成交额** 的多层级分析能力，并通过 **Web 看板** 展示 **近 5 个交易日** 的趋势与对比。

| 层级 | 指标 | 看板入口 |
|------|------|----------|
| 全 A | 成交额、主买、主卖 | 页面 1 |
| 申万板块（**二级**，约 131 行） | 成交额、占比、主买、主卖 | 页面 2 Tab「申万二级」、3 |
| **热门概念 / 概念板块** | 同上（成份重叠，占比之和可 >100%） | 页面 2 Tab「热门概念」「概念板块」 |
| 个股 | 成交额、主买、主卖 | 页面 4（从页面 3 下钻） |
| ETF | 成交额、占 A 股比 | 页面 5、6 |
| 数据管理 | 定时任务、补数、导出、日历 | **页面 7（管理页）** |

**统一时间窗口**：看板页面 1–6 均为 **近 5 个交易日**（非自然日）。

---

## 2. 已完成能力（Phase 1）

### 2.1 数据源

| 角色 | 平台 | 认证 |
|------|------|------|
| 主数据 | [必盈 API](https://www.biyingapi.com/doc_hs) | `BIYING_LICENCE` |
| 部署 | 腾讯云 Ubuntu + Python 3.10 | `pip install -r requirements.txt` |

### 2.2 采集脚本

`scripts/fetch_by_daily.py` + `scripts/by_common.py`

| 步骤 | 接口 | 输出 |
|------|------|------|
| 全 A 列表 | `hslt/list` | 对照 / 未归类 |
| 申万映射 | `hszg/list` + `hszg/gg`（**type2=1 二级**） | `sector_stock_mapping.csv` |
| 个股成交 | `hsrl/ssjy_more` | `turnover` |
| 个股买卖 | `hsstock/history/transaction` | `active_buy` / `active_sell` |
| 板块 / 全 A 汇总 | 本地聚合 | `sector_*_daily.csv`、`market_summary_daily.csv` |
| ETF 成交 | `fd/list/etf` + `fd/real/time` | `etf_turnover_latest.csv`（**无买卖**） |

### 2.3 板块层级（申万）

| 层级 | 必盈 `type2` | 板块数 | 默认 |
|------|-------------|--------|------|
| 一级 | `0` | 31 | 否（已弃用为看板默认） |
| **二级** | `1` | **131** | **是** |
| 三级 | — | 0 | 必盈无 L3 |

- 默认采集与看板展示：**申万二级**（`scripts/sector_config.py` → `DEFAULT_SECTOR_LEVEL=l2`）。
- 全 A 覆盖率：约 **99.81%**（5198/5208）；约 10 只新股/未入库成份无 L2 归属，与 L1 未归类集合相同。
- 已有 L1 历史库数据：运行 `build_sector_mapping.py --level l2` + `migrate_sectors_to_l2.py` 可**不重打 API** 迁移板块归属并重聚合。
- **sector / stock 一致**：落库或重聚合后自动 **删除** 当日 `sector_daily` 中无对应 `stock_daily` 成份的僵尸行（如 L1 残留 `sw_ysjs`）；亦可运行 `rebuild_sector_aggregates.py` 一键修复。

### 2.4 数据一致性（sector ↔ stock）

| 规则 | 说明 |
|------|------|
| 聚合来源 | `sector_daily` 由 `stock_daily` 按 `sector_code` 汇总 |
| 下钻条件 | 个股页按 `sector_code` 精确匹配；**两边 code 必须一致** |
| L1→L2 | L2 无「有色金属」等 L1 code；迁移后须清理 sector 僵尸行 |
| 修复方式 | 管理页 **成功** 重拉该日；或 `rebuild_sector_aggregates.py` |

### 2.5 当前局限

| 局限 | 影响 |
|------|------|
| 历史不足 5 日 | 看板展示实际 N 日并提示 |
| 必盈无 ETF 买卖 | ETF 页仅成交额 |
| 必盈无申万三级 | L3 需 BigQuant/AKShare 等（Phase 4） |
| 概念板块重叠 | 同一股可属多概念；概念 Tab 占比分母仍为全 A，各概念占比之和不必等于 100% |

### 2.6 概念板块（v3.6）

| 项 | 说明 |
|----|------|
| 映射 API | `hszg/list`（type2=2/3）+ `hszg/gg/{板块code}`（**板块→股**，与申万 L2 同路径） |
| 映射缓存 | `concept_mapping_hot.json` / `concept_mapping_board.json` + SQLite `concept_stock_map` |
| 日聚合表 | `concept_sector_daily`（含 8 档主买/主卖原子成交额字段） |
| 映射定时 | 默认 **02:00** `refresh_sector_mappings.py`（申万 L2 + 热门概念 + 概念板块） |
| 行情定时 | 默认 **21:35** `fetch_by_daily`（含概念聚合落库） |
| 个股原子字段 | `stock_daily` 落库 `zmbtdcje`…`zmsxdcje`（特大/大/中/小 × 主买/主卖）；`active_buy/sell` 仍为四档之和 |
| 历史回填 | `rebuild_sector_aggregates.py` 从 `stock_daily` + `concept_stock_map` 重算概念聚合（无需重拉 mapping API） |
| 手动补数上限 | 单次 **≤400 个交易日**（常用 365） |

---

## 3. 看板通用规则

### 3.1 数值单位

所有金额类展示（图表轴、表格、Tooltip）按数值大小 **自动选用单位**：

| 绝对值范围 | 单位 | 示例 |
|------------|------|------|
| ≥ 1 亿 | **亿** | `123.45 亿` |
| ≥ 1 千万且 < 1 亿 | **千万** | `876.5 千万` |
| ≥ 1 万且 < 1 千万 | **万** | `456.7 万` |
| < 1 万 | **元** | `9,876 元` |

- 存储与 API 返回一律 **元（float）**；格式化仅在前端展示层。
- 占比：**百分比**，保留 2 位小数，如 `12.34%`。
- 同一页面内图表 Y 轴、表格、Tooltip 共用一套格式化函数。

### 3.2 交易日口径

- 「近 5 日」= 数据库中最近 **5 个交易日**（按 `trade_date` 降序取 5 条）。
- 某日无数据则跳过，不插零；不足 5 日时展示已有全部日期，页脚标注「当前 N 个交易日」。
- 必盈买卖数据 **21:30** 更新；默认定时 **21:35**，保证买卖与成交可对齐。

### 3.3 占比口径

| 场景 | 公式 |
|------|------|
| 板块占 A 股（页面 2） | `sector.turnover / market.turnover` |
| 板块净值占全 A（页面 2） | `(sector.active_buy - sector.active_sell) / (market.active_buy - market.active_sell)` |
| 成份股占板块（页面 4） | `stock.turnover / sector.turnover`（买卖/净值同理，分母为板块内合计） |
| ETF 占 A 股（页面 5） | `etf.turnover / market.turnover` |

分母均为 **当日全 A 成交额**（`market_daily.turnover`），非 ETF 市场合计。

### 3.4 导航结构

```text
页面 1  全 A 概览
页面 2  板块表格 ──► 页面 4  板块成份股 ──► 页面 4b  个股堆积图
页面 3  板块图表（accordion，可跳转页面 4）
页面 5  ETF 表格 ──► 页面 5b  ETF 成交额图
页面 7  管理（定时 / 补数 / 导出 / 数据日历）
```

**顶栏导航（建议）**

```text
[概览] [板块表格] [板块图表] [ETF 表格] [管理]
```

- 页面 2 **点击板块名称** → 页面 4 `/sector-stocks.html?sector=…&days=…&sort=…`。
- 页面 4 **点击个股名称** → 页面 4b `/stock-detail.html?stock=…&sector=…`。
- 页面 4 面包屑：`板块表格 > {板块名} 成份股`；页面 4b：`板块表格 > {板块名} > {代码} {名称}`。

---

## 4. 页面需求明细

### 4.1 页面 1：全 A 概览

**路由**：`/`

| 序号 | 图表 | 类型 | 时间 | 数据字段 |
|------|------|------|------|----------|
| 1 | A 股成交额 | 柱状图 | 近 5 交易日 | `market_daily.turnover` |
| 2 | A 股买入额 | 柱状图 | 近 5 交易日 | `market_daily.active_buy` |
| 3 | A 股卖出额 | 柱状图 | 近 5 交易日 | `market_daily.active_sell` |

**交互**：X 轴为交易日期；Y 轴自动单位；Tooltip 显示格式化金额。

**布局**：三图纵向排列，桌面单栏全宽，移动端可纵向滚动。

---

### 4.2 页面 2：板块每日排行榜（卡片列）

**路由**：`/sectors-table.html?kind=sw_l2|hot|board`（默认 `sw_l2`）

**Tab（页内平级）**

| Tab | `kind` | 数据表 |
|-----|--------|--------|
| 申万二级 | `sw_l2` | `sector_daily` |
| 热门概念 | `hot` | `concept_sector_daily`（type2=2） |
| 概念板块 | `board` | `concept_sector_daily`（type2=3） |

三个 Tab **布局、排序、搜索、跨列高亮、下钻成份股** 与现 L2 一致。概念 Tab 页脚注明：**成份重叠，占比之和不必等于 100%**；占比分母仍为 **当日全 A**。零成交概念仍展示（全 0，排末尾）。

**路由**：`/sectors/table`（旧链接可 302 至 html）

**布局**

- **列 = 交易日**，**从左到右 = 由近到远**（最新日在最左）。
- 每列内纵向堆叠该日全部申万 **二级** 板块（约 131 张）**小卡片**。
- 页面 **整体纵向滚动**（各列同步，不做列内独立滚动）；列数多时可 **横向滚动**。
- 默认 **近 5 交易日**；可选 **5 / 15 / 30** 日。

**单卡字段**

| 字段 | 说明 |
|------|------|
| 名次 | `#1` … `#131`（按当前排序规则） |
| 板块名称 | 申万二级名称 |
| 成交金额 + 成交占比 | 占比 = 板块 / **当日全 A 成交额** |
| 买入金额 + 买入占比 | 占比 = 板块主买 / **当日全 A 主买** |
| 卖出金额 + 卖出占比 | 占比 = 板块主卖 / **当日全 A 主卖** |
| **净值 + 净值占比** | 净值 = 主买 − 主卖；占比 = 板块净值 / **当日全 A 净值**；**正数红色、负数绿色** |

**排序**（顶部单一下拉，**对每一列分别生效**，用该列当日数据排序；**默认成交占比 ↓**）

| 选项 |
|------|
| 成交占比 ↓ / ↑ |
| 买入占比 ↓ / ↑ |
| 卖出占比 ↓ / ↑ |
| **净值 ↓ / ↑** |

**交互**

- **点击卡片空白区域**：该板块在 **所有列** 中对应卡片 **高亮**；再次点击同一卡片 → **取消高亮**；点击其他板块卡片 → **切换高亮**（**搜索激活时暂停**跨列高亮）。
- **点击卡片内板块名称**：跳转 **页面 4** 查看该板块成份股（`/sector-stocks.html?sector=…&days=…&sort=…`），携带当前天数与排序。
- 用途：横向对比每日排行；需要看个股时从名称下钻。

**搜索**（固定顶栏，纯前端）

| 项 | 说明 |
|----|------|
| 范围 | 板块 **名称**（去前缀后），包含匹配、不区分大小写 |
| 结果 | 显示 `找到 N 个`；`上一个` / `下一个` 循环定位 |
| 顺序 | **日期今→远**（列从左到右），每列内 **上→下**（当前排序） |
| 定位 | 纵向滚入视口 + 横向滚到对应日期列 |
| 高亮 | **全部** 匹配卡名称 `<mark>` 高亮；**当前项** 额外卡片边框强调 |
| 切换排序/天数 | 保留关键词，重新计算结果，索引回到第 1 个 |

**数据**：`sector_daily` + `market_daily`（买卖占比分母）。

### 4.3 页面 3：板块近 5 日图表

**路由**：`/sectors/charts`

每个申万 **二级** 板块一个 **可折叠卡片（Accordion）**：

```text
▼ 银行                              [查看个股]
    ├─ 柱状图：近 5 日成交额
    ├─ 柱状图：近 5 日买入额（主买）
    └─ 柱状图：近 5 日卖出额（主卖）
▶ 电子                              [查看个股]
```

| 项 | 说明 |
|----|------|
| 折叠 | 默认 **全部折叠**；展开时才渲染图表（懒加载） |
| 查看个股 | 跳转页面 4 |
| 板块顺序 | 与页面 2 当前排序一致（URL 传递 `sort`） |

**数据**：`sector_daily` 近 5 日：`turnover`、`active_buy`、`active_sell`。

---

### 4.4 页面 4：板块成份股每日排行（卡片列）

**路由**：`/sector-stocks.html?sector={sector_code}`

**入口**：页面 2 点击板块名称；页面 3「查看个股」按钮（携带 `sector`）。

**布局**（与页面 2 对称）

- **列 = 交易日**，**从左到右 = 由近到远**（最新日在最左）。
- 每列纵向堆叠该板块 **全部** 成份股小卡片（约 50–170 张/列）。
- **固定顶栏**（标题、排序、天数、日期栏）+ 卡片区独立纵向滚动；列多时 **横向滚动**，日期栏与卡片区 sync。
- 天数：**5 / 15 / 30**，默认 5；排序默认 **成交占比 ↓**。

**单卡字段**

| 字段 | 说明 |
|------|------|
| 名次 | `#1` … `#N`（每列独立） |
| 代码 + 名称 | 名称可点击 → **页面 4b** |
| 成交金额 + 成交占比 | 占比 = 个股 / **当日板块成交额** |
| 买入 + 买入占比 | 占比 = 个股主买 / **当日板块主买** |
| 卖出 + 卖出占比 | 占比 = 个股主卖 / **当日板块主卖** |
| **净值 + 净值占比** | 净值 = 主买 − 主卖；占比 = 个股净值 / **当日板块净值**；**正红负绿** |

**排序**（6+2 档，对每一列分别生效）

| 选项 |
|------|
| 成交占比 ↓ / ↑（默认 ↓） |
| 买入占比 ↓ / ↑ |
| 卖出占比 ↓ / ↑ |
| **净值 ↓ / ↑** |

**交互**

- **点击卡片空白**：该股在所有列高亮/取消/切换（同页面 2；**搜索激活时暂停**跨列高亮）。
- **点击个股名称**：跳转页面 4b（`/stock-detail.html?stock=…&sector=…&days=…&sort=…`）。
- **返回板块表格**：顶栏「← 返回板块表格」+ 面包屑 `板块表格 > {板块名} 成份股`；链接保留 `days`、`sort`。

**搜索**（规则同页面 2；匹配 **股票代码 + 名称**）

**数据**：`stock_daily` 按 `sector_code` 过滤；占比分母为 **板块内** 当日合计。

---

### 4.4b 页面 4b：个股买卖堆积图

**路由**：`/stock-detail.html?stock={code}&sector={sector_code}`

**入口**：页面 4 点击个股名称。**顶栏导航不新增入口**（仅下钻）。

**页头**

- **← 返回成份股** + 面包屑：`板块表格 > {板块名} > {代码} {名称}`。
- 天数下拉 **5 / 15 / 30**（默认继承来源页 `days`）。

**图表**

- 类型：**堆积柱状图**（主买 + 主卖 堆叠；柱高 = 成交额，口径上主买+主卖=成交额）。
- X 轴：**左新右旧**（与列表页一致）。
- Y 轴：金额自动单位；Tooltip 展示日期、成交额、主买、主卖。
- 主买段 **红色**、主卖段 **绿色**（与净值涨跌色一致：买为红、卖为绿）。

**返回链**

| 当前页 | 返回 |
|--------|------|
| 4b 个股图 | 4 成份股（保留 sector/days/sort） |
| 4 成份股 | 2 板块表格（保留 days/sort） |

**数据**：`GET /api/stocks/{code}/series?days=&sector=`

---

### 4.5 页面 5：ETF 每日排行（卡片列）

**路由**：`/etf-table.html`

**布局**（与页面 2 对称）

- **列 = 交易日**，**从左到右 = 由近到远**；每列纵向堆叠 **全部** ETF（约 1480 张/列）。
- **固定顶栏** + 卡片区独立纵向滚动；列多时横向滚动，日期栏 sync。
- 天数：**5 / 15 / 30**，默认 5；**全量展示**（与页面 2 一致，不做分页）。

**单卡字段**

| 字段 | 说明 |
|------|------|
| 名次 | `#1` … `#N`（每列独立） |
| 代码 + 名称 | 名称可点击 → **页面 5b** |
| 成交金额 + 成交占比 | 占比 = ETF / **当日全 A 成交额** |

**无** 买入 / 卖出 / 净值（必盈暂无 ETF 买卖数据）。

**排序**（4 档，每列独立；默认 **成交占比 ↓**）

| 选项 |
|------|
| 成交占比 ↓ / ↑ |
| 成交额 ↓ / ↑ |

**搜索**（规则同页面 2/4：代码+名称、今→远定位、上/下一个、文字高亮；搜索时暂停跨列高亮）

**交互**

- **点击卡片空白**：该 ETF 在所有列高亮/取消/切换。
- **点击 ETF 名称**：跳转 **页面 5b** 查看近 N 日成交额柱图。

**数据**：`etf_daily` + `market_daily`（占比分母为全 A）。

---

### 4.5b 页面 5b：ETF 成交额柱图

**路由**：`/etf-detail.html?etf={code}`

**入口**：页面 5 点击 ETF 名称；顶栏 **不新增** 入口。

- **← 返回 ETF 表格** + 面包屑：`ETF 表格 > {代码} {名称}`
- 天数 **5 / 15 / 30**（继承来源页）
- **单序列柱状图**：近 N 日成交额；X 轴 **左新右旧**

**数据**：`GET /api/etfs/{code}/series?days=`

**说明**：必盈 **暂无 ETF 持仓/成份股** 接口，本期不做持仓下钻。

---

### 4.6 页面 6：ETF 图表（已废弃）

原 Accordion 批量看图页（`/etf-charts.html`）**不再使用**；功能由页面 5 + 5b 替代。

---

### 4.7 页面 7：数据管理

**路由**：`/admin`  
**权限**：当前为个人自用，管理页与 `/api/admin` **无额外鉴权**（与看板同端口开放）。

管理页集中提供 **采集调度、手动补数、数据导出、数据日历** 四类能力，并统一展示 **任务状态与报错信息**。

#### 4.7.1 定时更新配置

| 项 | 说明 |
|----|------|
| 功能 | 配置 **每天自动更新** 当日（最新可用 `trade_date`）全量数据的执行时刻 |
| 默认时间 | **21:35**（Asia/Shanghai） |
| 执行日类型 | **交易日**（默认）或 **自然日**；管理页可切换 |
| 配置项 | 时、分（24 小时制）；是否启用；执行日类型（`trading_day` / `calendar_day`） |
| 交易日模式 | 仅在 **A 股交易日** 触发；周末与法定休市日跳过 |
| 自然日模式 | **每天**到点触发（含周末/节假日）；适用于测试或强制补跑 |
| 更新范围 | 与 `fetch_by_daily.py` 一致：行业映射缓存、个股成交/买卖、板块汇总、全 A 汇总、ETF 成交，并 UPSERT `history.db` |
| 状态展示 | 最近一次定时任务：开始/结束时间、目标 `trade_date`、状态（成功/失败/运行中）、耗时 |
| 报错展示 | 失败时展示 **错误摘要** + **日志尾部**（可展开完整 log）；支持复制 |

**说明**

- 界面「当日」指任务运行时 API 能提供的 **目标交易日期**（`trade_date`），写入 DB 以该日期为键。
- **21:35** 为产品缺省，与必盈买卖 21:30 更新衔接；管理员可在管理页调整。

#### 4.7.2 手动指定日期 / 区间补数

| 项 | 说明 |
|----|------|
| 功能 | 填写 **开始日期** 与 **结束日期**（均为 `YYYY-MM-DD`，**两项必填**），手动触发该闭区间内 **全部 A 股交易日** 的数据采集并写入历史库 |
| 单日 | **开始 = 结束** 时视为补 **1 个交易日**（与旧版「选一天」等价） |
| 区间展开 | 系统用 SSE 交易日历计算 `[开始, 结束]` 内交易日列表；自然日中的周末/休市 **自动跳过** |
| 预览 | 提交前展示「共 **N** 个交易日」及首尾日期；**N = 0** 时禁止提交 |
| 覆盖策略 | **不跳过** 库内已有完整数据的日期；区间内每个交易日 **全量重拉并 UPSERT 覆盖** |
| 校验 | 见下表；管理页对开始/结束日分别显示「✓ 交易日 / ✗ 休市」（仅提示，不阻止区间边界为休市日） |
| 场景 | 新库初始化、长假后批量补漏、定时任务连续失败后一次性回填 |
| 交互 | 开始日期 + 结束日期 +「开始更新」；运行中可 **取消** 任务 |
| 状态展示 | 任务队列：ID、日期范围、触发方式、状态（含 `cancelled`）、进度、耗时 |
| 报错展示 | 同 4.7.1；失败任务可 **一键重试**（仍校验区间） |
| 并发 | 同一时刻 **仅 1 个采集任务**；第二个返回 409 |

**输入校验**

| 规则 | 行为 |
|------|------|
| 开始、结束均必填 | 缺任一项 → 400 |
| 开始 ≤ 结束 | 否则 → 400「结束日期不能早于开始日期」 |
| 结束 ≤ 今天 | 不允许补未来日期 → 400 |
| 区间内交易日数 | **1 ~ 30**（上限可配置）；超出 → 400 |
| 区间内无交易日 | → 400「所选区间无 A 股交易日」 |
| 单日且非交易日 | **拒绝**（休市无行情/资金流，补数无意义） |
| 区间边界为休市日 | **允许**（自然日范围可跨周末；实际只补中间交易日） |

**数据拉取口径（区间内每个交易日）**

| 数据 | 接口策略 |
|------|----------|
| 买卖 | 按股 `history/transaction?st=YYYYMMDD&et=YYYYMMDD`（区间一次拉取后按日拆分）；实现见方案文档 |
| 成交额 | 按股日 K `hsstock/history/{code}/d/n?st=&et=` |
| 当日（区间含今天） | 今天可走 `fetch_by_daily` 实时路径；历史日用 K 线 + transaction |
| ETF | 仍仅 **今天** 可 API 补；历史 ETF 不能通过区间补数批量获取，任务日志 **warning**，该日 `etf_daily` 可能仍为空 |
| 落库 | 按日 UPSERT 四表；sector 写入后 **prune** 僵尸行（同 v3.4） |

**任务进度（区间）**

- `progress` 示例：`stocks 1200/5208` 或 `dates 3/8`
- 任务记录 **日期** 列展示：`2026-06-01 ~ 2026-06-12`（单日则 `2026-06-12`）

**与 `backfill_history.py` 区别**

| | 区间补数（4.7.2） | `backfill_history --days N` |
|--|-------------------|----------------------------|
| 日期范围 | 用户指定起止 | 从今天往前 N 个交易日 |
| 入口 | 管理页 | CLI |
| 跳过已有 | **否**，全量覆盖 | 否（UPSERT 覆盖） |

#### 4.7.3 按日下载全量数据

| 项 | 说明 |
|----|------|
| 功能 | 选择某一已有数据的 `trade_date`，下载 **该日全部数据** 为文件包 |
| 格式 | **ZIP**，内含 UTF-8 **CSV** + `README.txt` |
| 文件清单 | `market_daily.csv`（1 行）、`sector_daily.csv`、`stock_daily.csv`、`etf_daily.csv`、`meta.json`（行数、生成时间、trade_date） |
| 交互 | 日期下拉（仅列出已有数据的日期）+「下载」按钮 |
| 空数据 | 该日无记录时按钮禁用并提示 |

#### 4.7.4 已有数据日期日历

| 项 | 说明 |
|----|------|
| 功能 | 展示系统中 **已落库** 的日期列表及完整度，便于掌握数据情况 |
| 展示形式 | **日历视图** + **列表视图**（可切换） |
| 每个日期显示 | `trade_date`；是否有 market / sector / stock / etf；各行数；最近更新时间 |
| 完整度标识 | ✅ 完整（四表均有且 stock_count > 0）／⚠ 部分（缺 ETF 或买卖）／❌ 失败任务残留 |
| 交互 | 点击某日 → 快捷 **下载** 或 **重新采集**（跳转 4.7.2 并预填开始/结束为同一日） |

---

## 5. 数据需求（Phase 2）

看板统一依赖 **近 5 交易日** 历史，须先建设历史层。

### 5.1 最少历史深度

| 用途 | 交易日数 |
|------|----------|
| 全部看板页面 1–6 | **5** |

### 5.2 逻辑数据模型

#### market_daily（1 行 / 日）

| 字段 | 说明 |
|------|------|
| `trade_date` | 主键 |
| `turnover` | 全 A 成交额（元） |
| `active_buy` | 全 A 主买 |
| `active_sell` | 全 A 主卖 |
| `net_active` | 主买 − 主卖 |
| `stock_count` | 有数据股票数 |

#### sector_daily（~131 行 / 日，申万二级）

| 字段 | 说明 |
|------|------|
| `trade_date`, `sector_code` | 联合主键 |
| `sector_name` | |
| `turnover`, `turnover_pct` | 成交额及占 A 股比 |
| `active_buy`, `active_sell`, `net_active` | |
| `stock_count` | |

#### stock_daily（~5200 行 / 日）

| 字段 | 说明 |
|------|------|
| `trade_date`, `stock_code` | 联合主键 |
| `stock_name`, `sector_code`, `sector_name` | |
| `turnover`, `active_buy`, `active_sell`, `net_active` | |

#### etf_daily（~1480 行 / 日）**新增**

| 字段 | 说明 |
|------|------|
| `trade_date`, `etf_code` | 联合主键 |
| `etf_name` | |
| `exchange` | SH / SZ |
| `turnover` | ETF 成交额（元） |
| `turnover_pct` | 占当日 A 股成交额比（冗余） |

#### fetch_jobs（采集任务日志）**新增**

| 字段 | 说明 |
|------|------|
| `job_id` | 主键（UUID） |
| `trade_date` | 目标交易日期 |
| `trigger` | `scheduled` / `manual` |
| `status` | `pending` / `running` / `success` / `failed` / `cancelled` |
| `started_at`, `finished_at` | |
| `duration_sec` | |
| `progress` | 可选文本，如「资金流 1200/5208」 |
| `error_message` | 失败摘要 |
| `log_path` | 完整日志文件路径 |

#### app_settings（应用配置）**新增**

| 键 | 说明 | 默认 |
|----|------|------|
| `schedule_enabled` | 是否启用定时 | `true` |
| `schedule_time` | 每日执行时刻 HH:MM | `21:35` |
| `schedule_timezone` | 时区 | `Asia/Shanghai` |
| `schedule_enabled` | 是否启用定时 | `true` |
| `schedule_run_mode` | 执行日类型 | `trading_day`（默认）或 `calendar_day` |

#### trading_calendar（交易日历，本地缓存）**新增**

| 字段 | 说明 |
|------|------|
| `trade_date` | 主键，YYYY-MM-DD |
| `is_trading` | 1=交易日，0=休市 |
| `source` | `pmc_sse` / `biying_kline` |
| `updated_at` | 同步时间 |

**交易日判定来源**（见 [BIYING_API.md §8](./BIYING_API.md)）：

- **主方案**：`pandas_market_calendars` 的 `SSE` 日历（`scripts/trading_calendar.py`），本地毫秒级、无需 API。
- **校验/兜底**：必盈日 K `hsstock/history/000001.SZ/d/n/...`（`verify` 子命令）。
- 可选写入 SQLite `trading_calendar` 表（`sync-db`）；调度器、看板「近 5 日」直接调 Python 函数。

### 5.3 采集策略

| 场景 | 做法 |
|------|------|
| **每日定时** | 管理页配置（默认 **21:35**、`trading_day`）→ 调度器判断当日是否执行 → `fetch_by_daily.py` |
| **手动区间补数** | 管理页触发 `fetch_by_range.py --start YYYY-MM-DD --end YYYY-MM-DD`（开始=结束即单日） |
| **手动指定日（兼容）** | 等价于 `--start D --end D` |
| **首次 5 日回填** | `backfill_history.py --days 5` |
| **自然积累** | 连续采集 **5 个交易日** 后看板数据完整 |

### 5.4 API 频率（每日增量，与 Phase 1 相同）

| 数据 | 请求量级 |
|------|----------|
| A 股成交 | ~261 批 ssjy_more |
| A 股买卖 | ~5208 次 transaction |
| ETF 成交 | ~1480 次 fd/real/time |

---

## 6. 非功能需求

| 项 | 要求 |
|----|------|
| 浏览器 | Chrome / Edge / Safari 最新两版 |
| 响应式 | 桌面优先；宽表横向滚动 |
| 性能 | 页面 3：**131** 板块 × 3 图，懒加载；页面 6：1480 ETF × 1 图，**必须**懒加载 + 分页/Top N |
| 安全 | 个人自用，管理页无 token；**BIYING_LICENCE 不进前端** |
| ETF 表格 | 1480 行需虚拟滚动或分页（每页 50–100） |
| 采集任务 | 单 worker 串行；长任务后台运行，前端轮询状态 |
| 日志 | 每任务独立 log 文件，保留最近 30 天 |

---

## 7. 交付物

### Phase 2（历史层）

| 交付物 | 说明 |
|--------|------|
| `data/history.db` | SQLite |
| `scripts/history_store.py` | 读写 / UPSERT |
| `scripts/backfill_history.py` | `--days 5` 回填 |
| `fetch_by_daily.py` 扩展 | 含 `etf_daily` 写入 |

### Phase 3（看板）

| 交付物 | 说明 |
|--------|------|
| `dashboard/index.html` | 页面 1 |
| `dashboard/sectors-table.html` | 页面 2 |
| `dashboard/sectors-charts.html` | 页面 3 |
| `dashboard/sector-stocks.html` | 页面 4 |
| `dashboard/stock-detail.html` | 页面 4b |
| `dashboard/etf-table.html` | 页面 5 |
| `dashboard/etf-detail.html` | 页面 5b |
| `api/` + `scripts/serve_dashboard.py` | API 与启动 |
| `dashboard/static/js/format.js` | 万/千万/亿 |

### Phase 3b（管理页）

| 交付物 | 说明 |
|--------|------|
| `dashboard/admin.html` | 页面 7 |
| `scripts/scheduler.py` | 可配置定时调度 |
| `scripts/fetch_by_date.py` | 指定单日采集（内部可由区间脚本调用） |
| `scripts/fetch_by_range.py` | **区间补数**（管理页主入口） |
| `api/admin.py` | 调度 / 任务 / 导出 / 日历 API |
| `data/exports/` | 按日 ZIP 缓存（可选，gitignore） |

---

## 8. 已确认决策

| 项 | 决策 |
|----|------|
| 看板时间窗 | **统一近 5 交易日** |
| 主数据源 | 必盈 API |
| 行业 | **申万二级**（131 板块；必盈 无 L3） |
| 买卖口径 | 主买 / 主卖 |
| ETF | 仅成交额；占比相对 **A 股** |
| 历史存储 | SQLite |
| 图表库 | ECharts 5 |
| 默认定时 | **21:35** Asia/Shanghai，管理页可改 |
| 执行日类型 | 默认 **交易日**；可选 **自然日** |
| 交易日历 | **pandas_market_calendars（SSE）** + 必盈日 K 校验 |
| 任务状态 | `fetch_jobs` 表 + 日志文件；支持 **cancelled** |
| 手动补数 | **开始+结束必填**；区间内全量覆盖、**不跳过已有**；**仅补交易日**；单日选休市 **拒绝** |
| 区间上限 | 单次最多 **400 个交易日** |
| 强制补数 | **不提供**（休市日无行情/资金流，补数无意义） |
| sector 落库 | 写入后 **删除** 当日不在聚合结果中的 `sector_code`（防 L1 僵尸行） |
| 导出 CSV | **UTF-8 BOM**，Excel 中文不乱码 |
| 看板表格 | `.table-wrap` 内 sticky 表头；图表 Y 轴 `containLabel` 防裁切 |

---

## 9. 阶段规划

| 阶段 | 内容 | 状态 |
|------|------|------|
| Phase 1 | 单日 CSV 采集 | ✅ |
| Phase 2 | 5 日历史落库 + SQLite | ✅ |
| Phase 3 | 看板页面 1–6 | ✅ |
| **Phase 3b** | **管理页 7**（定时 / 补数 / 导出 / 日历 / 取消） | ✅ |
| **Phase 3c** | **区间补数**（起止日期、交易日展开、按股 st/et 拉取） | ✅ |
| Phase 4 | 导出 Excel、申万 L3、ETF 分类筛选 | 可选 |

---

## 10. 验收标准（Phase 3）

1. **页面 1**：三张柱图均为近 5 日，字段与 `market_daily` 一致，单位正确。
2. **页面 2**：5/15/30 日卡片列，左新右旧；八档排序各列生效；卡片含净值（正红负绿）；点击卡片跨列高亮；**点击名称**跳转成份股页；**搜索**板块名并上/下一个定位。
3. **页面 3**：每板块 3 图可折叠，「查看个股」进入页面 4。
4. **页面 4**：成份股卡片列（同页面 2 逻辑）；占比分母为板块内；含净值与排序；点击名称进入页面 4b；可返回页面 2；**搜索**代码/名称并定位。
4b. **页面 4b**：个股主买/主卖堆积柱图，X 轴左新右旧；可返回页面 4。
5. **页面 5**：ETF 卡片列全量展示，5/15/30 日，搜索定位，点击名称进入 5b 柱图。
5b. **页面 5b**：单 ETF 成交额柱图，可返回页面 5。
7. 历史不足 5 日时，各页展示实际天数并提示。

### 验收标准（Phase 3b 管理页）

1. 默认定时 **21:35**、执行日类型 **交易日**，修改后持久化并按新规则触发。
2. 手动选择日期触发采集，任务列表实时展示状态；失败可见错误信息与日志。
3. 已有数据的日期可下载 ZIP，内含 market / sector / stock / etf 四表 CSV。
4. 日历/列表正确展示已落库日期与各表行数；部分数据日期标 ⚠。
5. 并发提交两个手动任务时，第二个返回 409；非交易日提交返回 400。
6. 运行中任务可取消，状态变为 `cancelled`，子进程终止。

### 验收标准（Phase 3c 区间补数）

1. 管理页 **开始、结束日期均必填**；相等时补单日；不等时补闭区间内全部交易日。
2. 结束早于开始、结束晚于今天、区间内 0 个交易日、超过 400 个交易日 → 400 且前端提示明确。
3. 提交前展示「共 N 个交易日」预览；**不跳过** 库内已有完整数据的日期，成功后数据被覆盖。
4. 任务列表日期列展示区间；进度可观察（股票数或日序）；长任务可取消。
5. 区间仅含历史日时 A 股买卖+成交落库；含今天时今天走实时路径；历史 ETF 仍缺则标 ⚠。

---

## 11. 页面总览

| 页面 | 路由 | 对象 | 展示形式 | 图/表 |
|------|------|------|----------|-------|
| 1 | `/` | 全 A | 3 柱图 | 成交、买入、卖出 |
| 2 | `/sectors/table` | 板块 | **卡片列**（5/15/30 日） | 成交+买卖及占比；跨列高亮 |
| 3 | `/sectors/charts` | 板块 | Accordion | 每板块 3 柱图 |
| 4 | `/sectors/{code}/stocks` | 个股 | Accordion | 每股 3 柱图 |
| 5 | `/etf/table` | ETF | 表格 | 5 日 ×（额 + 占 A 股比） |
| 6 | `/etf/charts` | ETF | Accordion | 每 ETF 1 柱图 |
| 7 | `/admin` | 系统 | 表单 + 日历 + 任务列表 | 定时 / 补数 / 导出 / 数据日历 |
